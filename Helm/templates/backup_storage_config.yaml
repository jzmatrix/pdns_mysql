{{- $root := . -}}
{{- range .Values.dnsServers }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ $root.Values.serviceName }}-bu-pv-{{ .server }}
spec:
  capacity:
    storage: 100Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ $root.Values.serviceName }}-bu-storage-{{ .server }}
  hostPath:
    path: {{ $root.Values.backupPath }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - {{ .server }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $root.Values.serviceName }}-bu-pvc-{{ .server }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: {{ $root.Values.serviceName }}-bu-storage-{{ .server }}
  resources:
    requests:
      storage: 100Gi
---
{{- end }}  